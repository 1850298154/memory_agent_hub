### 工业界 RAG 核心架构解析

#### 1. 数据入库策略 (Indexing Strategy)

*   **Chunking (切片策略)**:
    *   **Naive**: 固定字符数（如 500字）。缺点是容易切断句子含义。
    *   **Recursive (工业标准)**: 也就是 LangChain 的 `RecursiveCharacterTextSplitter`。优先按段落切，不行按句子切，再不行按单词切。
    *   **Semantic Chunking (进阶)**: 利用 Embedding 计算相邻句子的相似度，相似度突变的地方作为切分点，保证语义完整性。
    *   **Overlap**: 必须有重叠（如 10%-20%），防止上下文在切分处丢失。

*   **Embedding Deduplication (去重)**:
    *   **场景**: 文档可能有多个版本，或者多个文档引用了同一段法律条文。向量库里存多份不仅浪费钱，还会导致检索出的 Top-K 全是重复内容，挤占 LLM 的上下文窗口。
    *   **做法**: 计算 Chunk 的 **MD5/SHA256 哈希值**。入库前检查 Redis 或 Bloom Filter，如果 Hash 存在则跳过 Embedding 计算。

#### 2. 检索策略 (Retrieval Strategy)

*   **Query Rewriting (查询重写)**:
    *   **场景**: 用户问题往往不完整（“它多少钱？” -> “iPhone 15 多少钱？”）或语义模糊。
    *   **做法**: 使用 LLM 将用户的 Query + 对话历史 重写为一个适合搜索引擎的独立 Query。或者做 **Query Expansion**（生成 3 个相关问题并行搜索）。

*   **Hybrid Search (双路召回)**:
    *   **痛点**: 向量检索（Semantic）擅长理解语义（“苹果”<->“水果”），但对专有名词、型号、ID（“Part-12345”）很不敏感。
    *   **BM25 (Lexical)**: 传统的关键词检索，擅长精确匹配。
    *   **融合 (Fusion)**: 使用 **RRF (Reciprocal Rank Fusion)** 倒数排名融合算法，将两路检索的排名统一打分。

*   **Rerank (重排序/精排)**:
    *   **痛点**: 召回阶段为了速度使用双塔模型（Bi-Encoder，向量点积），精度有限。召回 Top 50 可能混杂了很多相关但不准确的内容。
    *   **做法**: 使用 **Cross-Encoder** 模型（如 BGE-Reranker, Cohere）。它将 Query 和 Document **拼在一起** 输入模型，进行深度的注意力机制计算，输出相关性得分。虽慢但极准，仅对 Top 50 进行重排，取 Top 5 给 LLM。

---

### Python 代码实战实现

这段代码模拟了一个完整的工业级 RAG 检索流程。为了运行方便，使用了轻量级库模拟数据库行为。

**环境依赖**:
`pip install rank_bm25 sentence-transformers numpy scikit-learn`

```python
import hashlib
import numpy as np
from typing import List, Dict, Set
from rank_bm25 import BM25Okapi
from sentence_transformers import SentenceTransformer, CrossEncoder, util
import jieba # 简单的中文分词，实际生产中可能用 Elasticsearch 的 analyzer

class IndustrialRAG:
    def __init__(self):
        # 1. 加载模型
        # Bi-Encoder 用于生成向量 (速度快)
        print("Loading Bi-Encoder...")
        self.embed_model = SentenceTransformer('sentence-transformers/all-MiniLM-L6-v2') 
        # Cross-Encoder 用于 Rerank (精度高，速度慢)
        print("Loading Reranker...")
        self.rerank_model = CrossEncoder('cross-encoder/ms-marco-MiniLM-L-6-v2')
        
        # 模拟数据库
        self.chunks: List[Dict] = []
        self.chunk_hashes: Set[str] = set()
        self.vectors = None
        self.bm25 = None
        self.corpus_tokenized = []

    # --- 模块 1: Chunking & Deduplication ---
    def add_document(self, text: str, chunk_size=100, overlap=20):
        """
        简单的滑动窗口切片 + MD5去重
        """
        n = len(text)
        for i in range(0, n, chunk_size - overlap):
            chunk_text = text[i:i + chunk_size]
            
            # 【去重策略】计算 Hash
            chunk_hash = hashlib.md5(chunk_text.encode('utf-8')).hexdigest()
            if chunk_hash in self.chunk_hashes:
                continue # 跳过重复内容
            
            self.chunk_hashes.add(chunk_hash)
            self.chunks.append({"text": chunk_text, "id": len(self.chunks)})
            
            # 为 BM25 准备分词数据
            self.corpus_tokenized.append(list(jieba.cut(chunk_text)))

    def build_index(self):
        """
        构建向量索引和 BM25 索引
        """
        print(f"Indexing {len(self.chunks)} chunks...")
        texts = [c["text"] for c in self.chunks]
        
        # 1. 向量化 (Semantic)
        self.vectors = self.embed_model.encode(texts, convert_to_tensor=True)
        
        # 2. BM25 索引构建 (Lexical)
        self.bm25 = BM25Okapi(self.corpus_tokenized)

    # --- 模块 2: Query Rewriting (Mock) ---
    def rewrite_query(self, query: str, chat_history: List[str] = None) -> str:
        """
        模拟调用 LLM 进行 Query 重写。
        真实代码：
        response = openai.ChatCompletion.create(
            messages=[{"role": "system", "content": "重写用户问题使其更清晰..."}]
        )
        """
        # 假设这里 LLM 把 "它怎么用？" 结合历史重写为了 "Redis的位图功能怎么用？"
        # 为了演示，我们直接返回原 Query 或做简单扩充
        print(f"Orignal Query: {query}")
        rewritten_query = query # 实际中这里是 LLM 的输出
        print(f"Rewritten Query: {rewritten_query}")
        return rewritten_query

    # --- 模块 3: Hybrid Search (RRF Fusion) ---
    def hybrid_search(self, query: str, top_k=10):
        # 1. 向量检索 (Semantic Search)
        query_vec = self.embed_model.encode(query, convert_to_tensor=True)
        # 计算余弦相似度
        semantic_scores = util.cos_sim(query_vec, self.vectors)[0]
        # 获取 Top K 索引
        semantic_top_k_indices = np.argsort(semantic_scores.cpu().numpy())[::-1][:top_k]
        
        # 2. 关键词检索 (BM25)
        tokenized_query = list(jieba.cut(query))
        bm25_scores = self.bm25.get_scores(tokenized_query)
        bm25_top_k_indices = np.argsort(bm25_scores)[::-1][:top_k]
        
        # 3. RRF 融合 (Reciprocal Rank Fusion)
        # 公式: score = 1 / (k + rank)
        fusion_scores = {}
        k = 60 # RRF 常数
        
        # 处理向量排名
        for rank, idx in enumerate(semantic_top_k_indices):
            if idx not in fusion_scores: fusion_scores[idx] = 0
            fusion_scores[idx] += 1 / (k + rank + 1)
            
        # 处理 BM25 排名
        for rank, idx in enumerate(bm25_top_k_indices):
            if idx not in fusion_scores: fusion_scores[idx] = 0
            fusion_scores[idx] += 1 / (k + rank + 1)
            
        # 按融合分数排序
        sorted_indices = sorted(fusion_scores.items(), key=lambda x: x[1], reverse=True)
        
        # 返回去重后的候选集 (Retrieve Stage)
        candidates = [self.chunks[idx] for idx, score in sorted_indices]
        return candidates

    # --- 模块 4: Rerank (Cross-Encoder) ---
    def rerank(self, query: str, candidates: List[Dict], top_n=3):
        """
        使用 Cross-Encoder 进行精排
        """
        if not candidates:
            return []
            
        # 构造配对 [Query, Document Text]
        pairs = [[query, doc["text"]] for doc in candidates]
        
        # 计算相关性分数
        scores = self.rerank_model.predict(pairs)
        
        # 打包结果并排序
        results = []
        for i, score in enumerate(scores):
            results.append({
                "chunk": candidates[i],
                "score": score
            })
            
        # 按分数降序
        results.sort(key=lambda x: x["score"], reverse=True)
        return results[:top_n]

    # --- 完整的 Pipeline ---
    def search_pipeline(self, user_query: str):
        # 1. Query Rewrite
        final_query = self.rewrite_query(user_query)
        
        # 2. Hybrid Search (Recall Top 50)
        candidates = self.hybrid_search(final_query, top_k=10)
        print(f"Recall: Found {len(candidates)} candidates.")
        
        # 3. Rerank (Precision Top 3)
        final_results = self.rerank(final_query, candidates, top_n=3)
        
        return final_results

# --- 测试用例 ---

rag = IndustrialRAG()

# 模拟文档库 (包含语义模糊和专有名词)
docs = [
    "分布式系统的CAP定理指出，一致性、可用性和分区容错性无法同时满足。", # 语义相关
    "Redis 是一个开源的内存数据结构存储系统，通常用作数据库、缓存和消息代理。",
    "错误代码 502 Bad Gateway 表示网关或代理服务器从上游服务器收到了无效响应。", # 关键词
    "订单号 ORDER-9981 的退款流程已启动，预计24小时到账。", # 专有ID
    "CAP定理指出，一致性、可用性和分区容错性无法同时满足。", # 重复内容，测试去重
]

for doc in docs:
    rag.add_document(doc)

rag.build_index()

print("\n--- Test 1: 语义检索 ---")
results = rag.search_pipeline("如何保证分布式系统的一致性？")
for r in results:
    print(f"[Score: {r['score']:.4f}] {r['chunk']['text']}")

print("\n--- Test 2: 精确ID检索 (测试 BM25 作用) ---")
# 纯向量模型很难匹配到 ORDER-9981 这种无语义的ID，BM25 此时会起作用
results = rag.search_pipeline("查询订单 ORDER-9981 的状态")
for r in results:
    print(f"[Score: {r['score']:.4f}] {r['chunk']['text']}")
```

### 核心亮点解读

1.  **RRF 融合算法 (Reciprocal Rank Fusion)**:
    *   代码中 `fusion_scores[idx] += 1 / (k + rank + 1)` 这一行是关键。
    *   **面试话术**: “我们不直接加权两路的分数，因为 BM25 分数范围（0-无穷）和 Cosine 相似度（0-1）不在一个量级，归一化很难做。RRF 仅依赖**排名**，不依赖具体分数，鲁棒性更强，是目前 Elasticsearch 和 Cohere 推荐的标准做法。”

2.  **Embedding 去重**:
    *   代码中的 `hashlib.md5` 部分。
    *   **面试话术**: “在处理大量 PDF 文档时，页眉页脚、引用条款重复率很高。我们在入库前做 Hash 去重，不仅节省了 30% 的向量存储空间，更重要的是防止检索时 Top 5 全是同一句话的重复切片，保证了给 LLM 的上下文多样性。”

3.  **Cross-Encoder Rerank**:
    *   **面试话术**: “召回阶段用 Bi-Encoder（双塔）是为了快（毫秒级），但它只能捕捉模糊的语义。精排阶段我们引入 BGE-Reranker（Cross-Encoder），虽然它推理一次要几十毫秒，但我们只对 Top 20 做重排，对整体延迟影响很小，但能把 NDCG@10 指标提升 10% 以上。”

4.  **Query Rewriting**:
    *   虽然代码里 Mock 了，但必须提到。
    *   **面试话术**: “真实场景中，用户多轮对话经常省略主语。我们有一个轻量级的 LLM 专门负责把 Query + History 重写成独立 Query，这解决了指代消解问题（比如‘它’指代什么）。”

这套方案和代码展示了从**数据治理**到**多路召回**再到**精细化排序**的完整闭环，完全符合工业界高可用 RAG 系统的标准。# recall



## 什么是召回（Recall）

在推荐系统或搜索系统中，**召回（Recall）阶段**是整个推荐链路的**第一步**。
简单来说，它的任务是：

> 从海量候选集合中（可能是上亿条内容、商品、视频等），**快速筛选出一个相对较小但相关性较高的子集**，供后续排序模型（Ranking）精排使用。

---

### 举个例子

假设你在刷抖音：

* 平台每天有几亿条视频；
* 不可能对每条视频都算模型打分；
* 系统先通过召回阶段筛出几千条“可能感兴趣”的视频；
* 然后再用排序阶段模型精细计算得分、重新排序，最终展示几十条。

所以：

| 阶段            | 目的                   | 规模          | 特点                 |
| --------------- | ---------------------- | ------------- | -------------------- |
| **召回**        | 粗筛，快速找“可能相关” | 百万级 → 千级 | 速度优先，特征轻量   |
| **排序**        | 精排，细选最优结果     | 千级 → 百级   | 精度优先，模型复杂   |
| **重排 / 精排** | 个性化展示优化         | 百级 → 十级   | 考虑多样性、时效性等 |

---

## 二、召回阶段的核心目标

召回要解决的问题是 **“在巨大候选集中找到相关的子集”**。
因此，它通常兼顾以下目标：

1. **高覆盖**：不能漏掉太多用户可能喜欢的内容；
2. **高相关性**：召回的内容要与用户兴趣有较强关联；
3. **高效率**：要在毫秒级完成检索。

---

## 三、召回阶段的技术演进

召回技术的演进大致经历了以下几个阶段：

---

### **阶段1：基于规则 / 倒排索引的召回**

> 特征：可解释、简单，但不够智能。

* **关键词召回**：最早期搜索系统使用倒排索引（Inverted Index），通过关键词匹配召回内容；
* **规则召回**：如“看了A视频 → 推荐同作者的视频”，或“同标签内容召回”。

**代表技术**：ElasticSearch、Lucene 倒排索引。
**缺点**：无法理解语义，召回粗糙。

---

### **阶段2：协同过滤召回（Collaborative Filtering）**

> 特征：利用用户行为数据而非内容本身。

* **基于用户的CF**：找出“和你行为相似的用户”，召回他们喜欢的内容；
* **基于物品的CF**：找出“和你看过的内容相似的内容”。

**代表算法**：ItemCF、UserCF。
**优点**：能捕捉隐式兴趣。
**缺点**：冷启动、稀疏性问题严重。

---

### **阶段3：Embedding 向量召回（Deep Retrieval）**

> 特征：利用深度学习生成稠密向量，实现语义级匹配。

* **核心思想**：将用户和物品都映射到同一个向量空间；
* **相似度计算**：通过内积 / 余弦相似度；
* **技术支撑**：ANN（Approximate Nearest Neighbor）检索，如 HNSW、Faiss、ScaNN。

**典型模型**：

* YouTube DNN（双塔模型）
* DSSM / ESMM / MIND / StarSpace 等

**优点**：

* 能捕捉深层语义；
* 高效可扩展；
* 可多召回源融合（user tower / item tower）。

**缺点**：

* 特征训练成本高；
* 模型更新滞后会影响实时性。

---

### **阶段4：多路召回（Multi-Recall Fusion）**

> 特征：多源召回融合，提高覆盖与多样性。

在大规模系统（如抖音、淘宝、知乎）中，一个召回策略已无法满足所有需求，因此会：

* 同时使用**多种召回策略**：

  * 用户兴趣召回（embedding-based）
  * 热点召回（trending）
  * 新品召回（exploration）
  * 同类内容召回（content-based）
  * 关系召回（follow-based）

再通过**召回融合层（Recall Fusion Layer）**统一管理和合并。

---

### **阶段5：大模型与生成式召回（LLM-based / RAG Recall）**

> 特征：语义理解更强，召回粒度从“物品”到“概念”。

* 基于**大语言模型 embedding** 的召回；
* 或者通过 **RAG（Retrieval-Augmented Generation）** 方式，让召回与生成相结合；
* 可以在向量数据库中进行**语义搜索 / 个性化知识召回**。

**典型代表**：

* OpenAI Embeddings + FAISS / Milvus；
* 文心、智谱等模型 + 向量召回。

---

## 四、召回系统的架构演进图（概念）

```
[用户画像]         [物品特征库]
      │                   │
      │                   │
      └──> 多路召回模块 ──┘
             │
             ↓
       召回结果融合
             ↓
         排序模块
             ↓
        精排/重排模块
             ↓
         最终推荐列表
```

---

## 五、总结表

| 阶段          | 技术特征         | 优点       | 局限           |
| ------------- | ---------------- | ---------- | -------------- |
| 规则/倒排召回 | 基于关键词、标签 | 简单、可控 | 无法理解语义   |
| 协同过滤      | 基于行为相似     | 挖掘兴趣   | 稀疏性、冷启动 |
| 向量召回      | 深度语义表示     | 高相关性   | 计算复杂       |
| 多路召回      | 融合多策略       | 多样性高   | 融合权重复杂   |
| 大模型召回    | 语义理解强       | 智能化     | 成本高         |
