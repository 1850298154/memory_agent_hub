# https://zhuanlan.zhihu.com/p/1936447811899065042


# Sigmoid、Softmax、交叉熵损失、logit、Logistic、交叉熵、KL散度、信息熵、信息增益、信息增益比、Gini、MSE等等所有的关系是什么？计算公式是什么？用具体数据演示计算过程

要理清这些概念的关系，核心是先划分**核心角色**（概率映射工具、不确定性度量工具、优化目标工具），再通过“从原始分数到概率，再到损失计算”的逻辑链条串联，最后用统一数据案例演示所有计算过程。


## 一、先明确所有概念的核心分类与关系
### 1. 核心角色划分（一张表看懂）
| 类别                | 包含概念                | 核心作用                                  | 依赖关系                                  |
|---------------------|-------------------------|-------------------------------------------|-------------------------------------------|
| ① 原始分数          | Logit（对数几率）       | 概率的“实数域映射形式”（0~1概率→-∞~+∞）   | 是Logistic函数的输入，Sigmoid的逆映射      |
| ② 概率映射工具      | Sigmoid（Logistic函数）、Softmax | 将Logit/logits（-∞~+∞）转为[0,1]概率       | 输入是Logit/logits，输出是概率，供损失计算 |
| ③ 不确定性度量工具  | 信息熵、KL散度、交叉熵  | 衡量“概率分布的混乱程度”或“两个分布的差距” | 输入是真实概率和预测概率，输出是度量值     |
| ④ 损失函数（优化目标）| 交叉熵损失、MSE、Gini   | 量化模型预测误差，指导参数更新            | 交叉熵损失=信息熵+KL散度（简化后核心用交叉熵）；MSE直接对比预测值与标签 |
| ⑤ 特征选择工具      | 信息增益、信息增益比    | 衡量特征对降低不确定性的贡献              | 基于信息熵计算，与损失函数无直接关联      |

### 2. 核心逻辑链条（从模型输出到损失）
模型输出 logits（原始分数） → 经 Sigmoid/Softmax 映射为预测概率 → 用交叉熵/KL散度衡量“预测概率与真实标签分布”的差距 → 交叉熵损失（简化后的优化目标）指导模型更新  
（注：Logit是“概率→实数”的映射，是Sigmoid的逆操作，用于理解概率与实数的转换逻辑）


## 二、所有概念的计算公式
### 1. 基础概念（Logit、Sigmoid、Softmax）
| 概念       | 计算公式                                                                 | 适用场景                  |
|------------|--------------------------------------------------------------------------|---------------------------|
| Logit（对数几率） | \( \text{logit}(p) = \ln\left( \frac{p}{1-p} \right) \)（\( 0 < p < 1 \)） | 概率→实数域映射（理论工具） |
| Sigmoid（Logistic函数） | \( \sigma(x) = \frac{1}{1 + e^{-x}} \)（\( x \) 是Logit/logits）           | 二分类/多标签分类的概率映射 |
| Softmax    | \( \text{Softmax}(x_i) = \frac{e^{x_i}}{\sum_{k=1}^C e^{x_k}} \)（\( C \)是类别数） | 多分类的概率映射（概率和为1） |

### 2. 不确定性度量（信息熵、KL散度、交叉熵）
| 概念       | 计算公式                                                                 | 物理意义                                  |
|------------|--------------------------------------------------------------------------|-------------------------------------------|
| 信息熵（H） | \( H(P) = -\sum_{i=1}^C P(i) \ln P(i) \)（\( P \)是真实概率分布）         | 真实分布的“混乱程度”（越小越有序）        |
| KL散度（相对熵） | \( D_{KL}(P\|Q) = \sum_{i=1}^C P(i) \ln\left( \frac{P(i)}{Q(i)} \right) \)（\( Q \)是预测概率） | 预测分布相对于真实分布的“偏离程度”（非对称） |
| 交叉熵（CE） | \( CE(P,Q) = -\sum_{i=1}^C P(i) \ln Q(i) \)                               | 衡量两个分布的“匹配程度”（越小越匹配）    |
| 关键关系   | \( CE(P,Q) = H(P) + D_{KL}(P\|Q) \)                                       | 交叉熵=真实分布的信息熵+KL散度（优化时H(P)固定，最小化CE等价于最小化KL散度） |

### 3. 损失函数与特征选择
| 概念               | 计算公式                                                                 | 适用场景                  |
|--------------------|--------------------------------------------------------------------------|---------------------------|
| 交叉熵损失（单样本） | 二分类：\( \text{Loss} = -[y \ln \hat{y} + (1-y) \ln(1-\hat{y})] \)；多分类：\( \text{Loss} = -\sum_{i=1}^C y_i \ln \hat{y}_i \) | 分类任务（主流损失函数）  |
| MSE（均方误差）    | \( \text{MSE} = \frac{1}{n} \sum_{i=1}^n (y_i - \hat{y}_i)^2 \)（\( n \)是样本数） | 回归任务（或分类任务的简单替代） |
| Gini（基尼系数）   | \( \text{Gini}(D) = 1 - \sum_{i=1}^C \left( \frac{|D_i|}{|D|} \right)^2 \)（\( D \)是数据集，\( D_i \)是类别i的样本子集） | 决策树特征选择（衡量数据集纯度） |
| 信息增益（IG）     | \( IG(A) = H(D) - H(D\|A) \)（\( H(D) \)是原数据集熵，\( H(D\|A) \)是特征A划分后的条件熵） | 决策树特征选择（特征重要性） |
| 信息增益比（IGR）  | \( IGR(A) = \frac{IG(A)}{H_A(D)} \)（\( H_A(D) = -\sum_{v=1}^V \frac{|D_v|}{|D|} \ln \frac{|D_v|}{|D|} \)，\( V \)是特征A的取值数） | 解决信息增益对多取值特征的偏好 |


## 三、统一数据案例：演示所有计算过程
### 场景设定
以“商品意图识别”任务为统一案例：
- 多分类场景（3个类别）：C=3（类别1：查询订单，类别2：申请退货，类别3：咨询物流）；
- 单样本真实标签（one-hot编码）：\( y = [1, 0, 0] \)（仅属于“查询订单”）；
- 模型输出logits（全连接层原始分数）：\( [2.5, -1.0, 0.8] \)；
- 数据集辅助信息（用于Gini、信息增益）：假设数据集D共100个样本，类别分布为[60, 30, 10]（查询订单60个，申请退货30个，咨询物流10个）；特征A（如“是否含‘订单’关键词”）有2个取值：A1（含关键词，60个样本，类别分布[50,5,5]）、A2（不含关键词，40个样本，类别分布[10,25,5]）。

### 步骤1：计算Logit、Sigmoid、Softmax
#### （1）Logit计算（以真实标签概率为例）
真实标签概率 \( P = [1, 0, 0] \)（多分类中one-hot标签即真实概率分布），取类别1的概率 \( p=1 \)（实际计算用接近1的值避免ln(0)）：
\[
\text{logit}(p=0.99) = \ln\left( \frac{0.99}{1-0.99} \right) = \ln(99) ≈ 4.595
\]
（意义：将0.99的概率映射为4.595的实数，与模型logits[2.5]趋势一致）

#### （2）Sigmoid计算（多标签场景演示，本例多分类用Softmax）
对logits每个元素应用Sigmoid：
\[
\hat{y}_1 = \frac{1}{1+e^{-2.5}} ≈ 0.924, \quad \hat{y}_2 = \frac{1}{1+e^{1.0}} ≈ 0.269, \quad \hat{y}_3 = \frac{1}{1+e^{-0.8}} ≈ 0.690
\]
（结果：每个类别独立概率，和无需为1，适用于多标签）

#### （3）Softmax计算（多分类场景，核心用这个）
将logits转为概率和为1的预测分布：
\[
\sum e^{x_i} = e^{2.5} + e^{-1.0} + e^{0.8} ≈ 12.182 + 0.368 + 2.225 ≈ 14.775
\]
\[
\hat{y}_1 = \frac{12.182}{14.775} ≈ 0.825, \quad \hat{y}_2 = \frac{0.368}{14.775} ≈ 0.025, \quad \hat{y}_3 = \frac{2.225}{14.775} ≈ 0.150
\]
（结果：预测概率 \( \hat{Q} = [0.825, 0.025, 0.150] \)，和为1）

### 步骤2：计算信息熵、KL散度、交叉熵
#### （1）信息熵（真实分布P的混乱程度）
真实概率 \( P = [1, 0, 0] \)（仅类别1为1，其他为0，分布极有序）：
\[
H(P) = -(1 \cdot \ln1 + 0 \cdot \ln0 + 0 \cdot \ln0) = -(0 + 0 + 0) = 0
\]
（意义：真实分布完全确定，混乱程度为0）

#### （2）KL散度（预测分布Q相对于真实分布P的偏离）
\[
D_{KL}(P\|Q) = 1 \cdot \ln\left( \frac{1}{0.825} \right) + 0 \cdot \ln\left( \frac{0}{0.025} \right) + 0 \cdot \ln\left( \frac{0}{0.150} \right) ≈ \ln(1.212) ≈ 0.192
\]
（意义：预测分布偏离真实分布的程度为0.192）

#### （3）交叉熵（P和Q的匹配程度）
根据关系 \( CE(P,Q) = H(P) + D_{KL}(P\|Q) \)：
\[
CE(P,Q) = 0 + 0.192 = 0.192
\]
直接计算验证：
\[
CE(P,Q) = -(1 \cdot \ln0.825 + 0 \cdot \ln0.025 + 0 \cdot \ln0.150) ≈ -(-0.192) = 0.192
\]
（意义：交叉熵越小，预测与真实越匹配）

### 步骤3：计算损失函数（交叉熵损失、MSE、Gini）
#### （1）交叉熵损失（多分类单样本）
多分类交叉熵损失直接等于交叉熵（因H(P)=0）：
\[
\text{Loss}_{CE} = CE(P,Q) ≈ 0.192
\]
（若真实标签是概率分布而非one-hot，需完整计算交叉熵）

#### （2）MSE（均方误差，假设用于分类任务的简单替代）
对比真实标签 \( y = [1,0,0] \) 和预测概率 \( \hat{y} = [0.825,0.025,0.150] \)：
\[
\text{MSE} = \frac{1}{3} \left[ (1-0.825)^2 + (0-0.025)^2 + (0-0.150)^2 \right]
\]
\[
= \frac{1}{3} \left[ 0.0306 + 0.0006 + 0.0225 \right] ≈ \frac{0.0537}{3} ≈ 0.018
\]
（意义：MSE衡量预测值与标签的平方差，分类任务中不如交叉熵损失高效）

#### （3）Gini系数（数据集D的纯度）
数据集D的类别分布占比：\( \frac{60}{100}=0.6, \frac{30}{100}=0.3, \frac{10}{100}=0.1 \)：
\[
\text{Gini}(D) = 1 - (0.6^2 + 0.3^2 + 0.1^2) = 1 - (0.36 + 0.09 + 0.01) = 0.54
\]
（意义：Gini越小，数据集纯度越高；这里0.54说明数据集有一定混乱度）

### 步骤4：计算信息增益、信息增益比（特征选择）
#### （1）计算条件熵H(D|A)（特征A划分后的平均熵）
- 特征A取值A1（60个样本）的熵：\( H(D1) = -( \frac{50}{60}\ln\frac{50}{60} + \frac{5}{60}\ln\frac{5}{60} + \frac{5}{60}\ln\frac{5}{60} ) ≈ 0.659 \)
- 特征A取值A2（40个样本）的熵：\( H(D2) = -( \frac{10}{40}\ln\frac{10}{40} + \frac{25}{40}\ln\frac{25}{40} + \frac{5}{40}\ln\frac{5}{40} ) ≈ 1.015 \)
- 条件熵：\( H(D\|A) = \frac{60}{100} \times 0.659 + \frac{40}{100} \times 1.015 ≈ 0.395 + 0.406 = 0.801 \)

#### （2）信息增益IG(A)
\[
IG(A) = H(D) - H(D\|A) = 0.851 - 0.801 = 0.05
\]
（注：原数据集D的熵H(D)需重新计算：\( H(D) = -(0.6\ln0.6 + 0.3\ln0.3 + 0.1\ln0.1) ≈ 0.851 \)）

#### （3）信息增益比IGR(A)
先计算特征A的固有值H_A(D)：
\[
H_A(D) = -\left( \frac{60}{100}\ln\frac{60}{100} + \frac{40}{100}\ln\frac{40}{100} \right) ≈ 0.971
\]
\[
IGR(A) = \frac{IG(A)}{H_A(D)} = \frac{0.05}{0.971} ≈ 0.052
\]
（意义：特征A的信息增益比为0.052，值越大说明该特征对分类的贡献越大）


## 四、所有概念的核心关系总结
1. **Logit与Sigmoid**：互逆操作（Logit：概率→实数；Sigmoid：实数→概率）；
2. **Sigmoid与Softmax**：都是概率映射工具（Sigmoid适用于二分类/多标签，Softmax适用于多分类）；
3. **信息熵、KL散度、交叉熵**：交叉熵=信息熵+KL散度，分类任务中信息熵固定，最小化交叉熵等价于最小化KL散度；
4. **交叉熵与交叉熵损失**：分类任务中，交叉熵损失直接复用交叉熵的计算（因真实标签是one-hot，信息熵为0）；
5. **MSE与交叉熵损失**：MSE适用于回归，交叉熵适用于分类（后者对分类任务的梯度更稳定）；
6. **Gini、信息增益、信息增益比**：都是特征选择工具，基于信息熵或数据集纯度，与模型训练的损失函数无直接关联。

通过统一案例可见：模型训练的核心链条是“logits→Softmax→预测概率→交叉熵损失”，其他概念要么是该链条的理论支撑（Logit、信息熵），要么是其他场景的工具（Gini、信息增益）。

# 模型输出 logits（原始分数） → 经 Sigmoid/Softmax 映射为预测概率 → 用交叉熵 / KL 散度衡量 “预测概率与真实标签分布” 的差距 → 交叉熵损失（简化后的优化目标）指导模型更新（注：Logit 是 “概率→实数” 的映射，是 Sigmoid 的逆操作，用于理解概率与实数的转换逻辑）。用具体数据的例子告诉我怎么流动和计算的


我们用 **“多分类意图识别”** 作为具体场景，完整演示从 `logits` 到模型更新信号的全流程，每个步骤都带详细计算，让你直观看到数据的流动和转换：

### 场景设定
- 任务：3分类意图识别（类别1：查询订单，类别2：申请退货，类别3：咨询物流）；
- 单样本真实标签（one-hot编码，即真实概率分布）：`y = [1, 0, 0]`（仅属于“查询订单”）；
- 模型全连接层输出（原始分数 logits）：`logits = [2.5, -1.0, 0.8]`（未经过任何激活，取值范围(-∞,+∞)）；
- 核心目标：通过流程计算得到“交叉熵损失”，作为模型参数更新的信号。


## 完整流程：logits → 预测概率 → 分布差距 → 损失 → 更新信号
### 步骤1：logits（原始分数）→ 预测概率（Softmax映射）
因为是**多分类任务**，用 `Softmax` 将 logits 映射为 [0,1] 区间、且总和为1的预测概率（多标签任务用 Sigmoid，逻辑类似）。

#### Softmax 计算公式：
对于每个类别的 logit \( x_i \)，预测概率 \( \hat{y}_i \) 为：
\[
\hat{y}_i = \frac{e^{x_i}}{\sum_{k=1}^C e^{x_k}} \quad (C=3，类别数)
\]

#### 具体计算：
1. 先计算每个 logit 的指数（\( e^x \)）：
   - \( e^{2.5} ≈ 12.182 \)（类别1的指数）；
   - \( e^{-1.0} ≈ 0.368 \)（类别2的指数）；
   - \( e^{0.8} ≈ 2.225 \)（类别3的指数）；
2. 计算指数和（分母）：
   \[
   \sum e^{x_k} = 12.182 + 0.368 + 2.225 ≈ 14.775
   \]
3. 每个类别的预测概率：
   - 类别1：\( \hat{y}_1 = \frac{12.182}{14.775} ≈ 0.825 \)（预测“查询订单”的概率82.5%）；
   - 类别2：\( \hat{y}_2 = \frac{0.368}{14.775} ≈ 0.025 \)（预测“申请退货”的概率2.5%）；
   - 类别3：\( \hat{y}_3 = \frac{2.225}{14.775} ≈ 0.150 \)（预测“咨询物流”的概率15%）；

#### 结果：预测概率分布 \( \hat{y} = [0.825, 0.025, 0.150] \)（总和=1，符合多分类要求）。


### 步骤2：用“交叉熵/KL散度”衡量“预测分布与真实分布的差距”
首先明确两个分布：
- 真实分布 \( P \)（由真实标签转化）：`[1, 0, 0]`（只有真实类别1的概率为1，其他为0）；
- 预测分布 \( Q \)（Softmax输出）：`[0.825, 0.025, 0.150]`。

我们需要量化这两个分布的“差距”，核心用 **交叉熵**（工程中直接用交叉熵作为损失，KL散度是其理论支撑）。

#### 先回顾关键公式：
1. KL散度（相对熵）：衡量 \( Q \) 相对于 \( P \) 的偏离程度：
   \[
   D_{KL}(P\|Q) = \sum_{i=1}^C P(i) \cdot \ln\left( \frac{P(i)}{Q(i)} \right)
   \]
2. 交叉熵（CE）：衡量两个分布的匹配程度，且 \( CE(P,Q) = H(P) + D_{KL}(P\|Q) \)（\( H(P) \) 是真实分布的信息熵）；
3. 信息熵 \( H(P) \)：真实分布的混乱程度（one-hot标签的信息熵为0，因为分布完全确定）：
   \[
   H(P) = -\sum_{i=1}^C P(i) \cdot \ln P(i)
   \]

#### 具体计算：
1. 计算真实分布的信息熵 \( H(P) \)：
   \[
   H(P) = -(1 \cdot \ln1 + 0 \cdot \ln0 + 0 \cdot \ln0) = -(0 + 0 + 0) = 0
   \]
   （注：\( \ln0 \) 理论上是-∞，但真实标签中0对应的项权重为0，所以整体为0）

2. 计算KL散度 \( D_{KL}(P\|Q) \)（预测分布相对于真实分布的偏离）：
   \[
   D_{KL}(P\|Q) = 1 \cdot \ln\left( \frac{1}{0.825} \right) + 0 \cdot \ln\left( \frac{0}{0.025} \right) + 0 \cdot \ln\left( \frac{0}{0.150} \right)
   \]
   简化后（只保留非0项）：
   \[
   D_{KL}(P\|Q) = \ln(1.212) ≈ 0.192
   \]
   （意义：预测分布比真实分布“混乱”0.192，偏离程度不大）

3. 计算交叉熵 \( CE(P,Q) \)：
   因为 \( H(P)=0 \)，所以 \( CE(P,Q) = H(P) + D_{KL}(P\|Q) = 0 + 0.192 = 0.192 \)；
   直接计算验证（工程中常用此方式）：
   \[
   CE(P,Q) = -\sum_{i=1}^C P(i) \cdot \ln Q(i) = -(1 \cdot \ln0.825 + 0 \cdot \ln0.025 + 0 \cdot \ln0.150) ≈ -(-0.192) = 0.192
   \]

#### 结果：两个分布的交叉熵为0.192（值越小，分布越匹配）。


### 步骤3：交叉熵损失（优化目标）→ 模型更新信号
在分类任务中，**交叉熵损失直接等于交叉熵**（因为 \( H(P) \) 是固定值，不影响参数更新方向），所以：
\[
\text{Loss}_{CE} = CE(P,Q) ≈ 0.192
\]

#### 损失的意义：
- 损失值0.192表示模型当前预测与真实标签的“误差大小”；
- 模型训练的目标是**最小化这个损失值**（理想情况下，损失趋近于0，此时预测概率 \( \hat{y} = [1, 0, 0] \)，完全匹配真实标签）。

#### 如何指导模型更新？
通过**反向传播算法**，计算损失函数对模型参数（如全连接层的权重矩阵 \( W \)、偏置 \( b \)）的梯度：
- 例如，损失对 logits 中类别1的梯度为：\( \frac{\partial \text{Loss}}{\partial x_1} = \hat{y}_1 - y_1 = 0.825 - 1 = -0.175 \)；
- 损失对类别2的梯度为：\( \frac{\partial \text{Loss}}{\partial x_2} = 0.025 - 0 = 0.025 \)；
- 梯度会告诉模型：“需要调整参数，让类别1的 logits 更大（因为梯度为负，参数更新方向相反），类别2、3的 logits 更小”；
- 模型通过梯度下降法（如SGD、Adam）更新参数后，下一轮训练的 logits 会更接近 `[∞, -∞, -∞]`，Softmax 后预测概率会更接近 `[1, 0, 0]`，损失值会更小。


## 流程总结（数据流动轨迹）
```
logits [2.5, -1.0, 0.8] 
→ 经Softmax映射 → 预测概率 [0.825, 0.025, 0.150] 
→ 与真实分布 [1, 0, 0] 计算 → 交叉熵 0.192 
→ 交叉熵损失 0.192 
→ 反向传播计算梯度 → 模型参数更新
```

### 补充：如果是二分类/多标签任务？
- 用 `Sigmoid` 替代 Softmax（每个类别独立映射概率，无需总和为1）；
- 交叉熵损失计算为“每个类别的二分类交叉熵之和”（如多标签案例中，损失=类别1交叉熵+类别2交叉熵+类别3交叉熵）；
- 核心逻辑一致：logits→概率→分布差距→损失→更新。

通过这个例子可以看出：整个流程的核心是“将模型的原始分数转化为可解释的概率，再通过分布差距量化误差，最终用误差指导模型优化”，所有概念都服务于“让模型预测更接近真实标签”这个目标。

