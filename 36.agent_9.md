# https://www.xiaohongshu.com/explore/69038e4e0000000005000984?xsec_token=ABewzZMNi1cUjH9iytlkeTv8ZIDBtrGSCy-G5IxpZEnkk=&xsec_source=pc_collect

# https://www.doubao.com/chat/27587463403320578

# 完整的技术总结 https://hustai.github.io/zh/posts/reasoning/LATS.html


### Agent九种设计模式详细对比表
| 设计模式                | 核心逻辑                                                                 | 优势                                                                 | 劣势                                                                 | 适用场景                                                                 |
|-------------------------|--------------------------------------------------------------------------|----------------------------------------------------------------------|----------------------------------------------------------------------|--------------------------------------------------------------------------|
| ReAct模式               | 思考（Reason）→ 行动（Act）→ 观察（Observe）循环，实时交互环境/工具       | 1. 交互性强，适配动态场景；2. 逻辑透明，可追溯每一步决策；3. 开发成本低 | 1. 无全局规划，复杂任务易跑偏；2. 依赖实时反馈，无反馈时效率低       | 信息检索、客服对话、简单工具调用（如查天气、查快递）                     |
| Plan and Solve模式      | 先拆解任务（Plan）→ 分步骤执行（Solve）→ 验证结果，聚焦“先规划后执行”     | 1. 结构化强，适配复杂任务；2. 目标明确，减少无效操作；3. 可复用规划逻辑 | 1. 规划阶段耗时长；2. 动态场景下规划易过时；3. 对模糊任务适配差       | 项目管理、论文写作、复杂数据分析（如多步骤数据清洗+可视化）             |
| LLMCompiler模式         | 以有向无环图（DAG）描述任务依赖，并行/串行调度工具/步骤，聚焦“任务编排”   | 1. 高效调度多工具/步骤；2. 支持并行执行，提升效率；3. 依赖清晰易维护   | 1. 学习成本高，需理解DAG逻辑；2. 简单任务overkill；3. 动态依赖难处理 | 多工具协同任务（如“爬取数据→清洗→分析→生成报告”）、工作流自动化         |
| Reason without Observation模式 | 纯LLM推理，不依赖外部工具/实时反馈，聚焦“无交互纯思考”                   | 1. 响应速度快，无外部依赖；2. 部署简单，无需集成工具；3. 低成本       | 1. 无法处理需实时数据/外部信息的任务；2. 复杂推理易出错；3. 无纠错机制 | 常识问答、逻辑推理（如数学证明）、静态内容生成（如写邮件模板）           |
| Basic Reflection模式    | 模拟“师生互动”：生成初稿→自我评审（Teacher角色）→ 修正（Student角色）循环 | 1. 无需外部反馈，自我迭代；2. 输出质量稳定；3. 逻辑闭环易落地         | 1. 评审标准依赖Prompt设计，易有偏见；2. 迭代次数有限，难突破认知边界 | 内容优化（如文案润色、论文修改）、代码重构、答案校验（如考试答题批改）   |
| Reflexion模式           | 基于强化学习（RL）：执行任务→记录反馈（成功/失败）→ 优化策略（Reward导向） | 1. 越用越优，适配长期任务；2. 能从失败中学习；3. 自主优化决策逻辑     | 1. 需大量样本训练，冷启动难；2. 训练成本高；3. 反馈机制设计复杂       | 长期迭代任务（如电商运营优化）、游戏AI、复杂问题求解（如算法竞赛）       |
| LATS模式                | 融合树搜索（Tree Search）+ ReAct + Plan&Solve + Reflection + RL，全链路优化 | 1. 通用性极强，适配各类复杂任务；2. 兼具规划、交互、迭代、优化能力；3. 容错率高 | 1. 开发/维护成本极高；2. 资源消耗大；3. 逻辑复杂易出bug               | 超复杂任务（如科研项目攻坚、大型工程设计）、高价值决策（如投资分析）     |
| Self-Discover模式       | 先反思任务本质（“我要解决什么？需要什么能力？”）→ 动态生成执行步骤       | 1. 适配模糊/开放任务；2. 自主补全任务边界；3. 灵活性强               | 1. 反思阶段耗时；2. 易过度发散，偏离核心；3. 依赖LLM抽象能力         | 创新任务（如“设计一款新型APP”）、开放域写作（如“写一篇未来科技短文”）、未知领域探索 |
| Storm模式               | 先生成全局大纲（Storming）→ 逐段填充细节（Detailing）→ 整合优化           | 1. 结构清晰，输出有条理；2. 适配长文本/复杂成果生成；3. 易协作修改   | 1. 大纲阶段定调，后续难调整；2. 细节易脱离大纲；3. 创造性不足       | 长文本写作（如书籍、报告）、方案设计（如活动策划案）、课程大纲+讲义生成 |


### 九种模式具体落地示例（含System Prompt、Agent设计、Tools、Memory）
#### 一、ReAct模式（思考-行动-观察）
**核心场景**：帮用户查询“北京2025年11月5日天气”+“推荐对应穿搭”  
- **System Prompt**：
  ```
  你是一个天气查询与穿搭推荐助手，遵循“思考→行动→观察→再思考”循环：
  1. 思考：用户需求是否需要外部工具？当前是否有足够信息？（如天气需查实时数据）
  2. 行动：调用指定工具获取信息（仅用天气查询工具），格式：【调用工具：天气查询，参数：城市=北京，日期=2025-11-05】
  3. 观察：接收工具返回结果，确认是否满足需求（如是否拿到温度、降雨概率）
  4. 最终：基于观察结果生成穿搭建议，无需额外工具则直接回答。
  工具说明：天气查询工具返回格式为{温度：xx℃，天气：xx，降雨概率：xx%}
  ```
- **Agent设计**：
  - 决策模块：判断是否需要调用工具（无天气数据则调用）
  - 执行模块：按格式调用天气工具，接收返回结果
  - 生成模块：结合天气数据输出穿搭建议
- **Tools**：天气查询API（输入城市+日期，返回温度/天气/降雨概率）
- **Memory**：短期记忆（仅存储当前用户需求、工具调用参数、返回结果）
- **示例流程**：
  1. 用户输入：“北京明天天气怎么样？该穿什么？”
  2. Agent思考：需要天气数据，调用工具→【调用工具：天气查询，参数：城市=北京，日期=2025-11-05】
  3. 工具返回：{温度：12℃，天气：晴，降雨概率：10%}
  4. Agent观察：数据完整→思考：需结合温度/天气推荐穿搭
  5. 输出：“北京2025年11月5日天气晴，12℃，降雨概率10%。建议穿薄羽绒服+毛衣，搭配围巾，舒适又保暖～”

#### 二、Plan and Solve模式（先规划后执行）
**核心场景**：帮用户完成“3天上海短途旅行攻略”  
- **System Prompt**：
  ```
  你是旅行规划助手，遵循“规划→执行→验证”流程：
  1. 规划：拆解用户需求为可执行步骤（如“确定出行日期→选景点→安排行程→推荐住宿/美食”），确保步骤完整、逻辑连贯。
  2. 执行：按步骤生成细节，需调用工具时明确说明（如查景点开放时间用“景点查询工具”）。
  3. 验证：检查行程是否合理（时间不冲突、路线顺、符合3天时长），不合理则调整。
  最终输出结构化攻略，含每日行程、交通、住宿、美食建议。
  ```
- **Agent设计**：
  - 规划模块：拆解任务为5步（确定核心需求→筛选景点→规划每日路线→推荐住宿/美食→校验合理性）
  - 执行模块：按步骤生成内容，必要时调用工具（如景点开放时间、交通路线）
  - 校验模块：检查行程是否冲突、时长是否匹配
- **Tools**：景点查询工具（开放时间/门票）、地图导航工具（路线耗时）、酒店预订工具（价格/位置）
- **Memory**：中长期记忆（存储规划步骤、各步骤结果、校验记录，支持步骤回溯修改）
- **示例流程**：
  1. 用户输入：“帮我做3天上海短途旅行攻略，喜欢人文景点和美食。”
  2. 规划模块：拆解步骤→①确定核心：人文+美食+3天；②筛选景点：豫园、外滩、田子坊、上海博物馆；③每日路线：Day1（豫园→外滩）、Day2（上海博物馆→田子坊）、Day3（武康路→陆家嘴）；④推荐住宿（人民广场附近）、美食（南翔小笼、本帮菜）；⑤校验：路线顺、无时间冲突。
  3. 执行模块：调用景点工具查开放时间，调用地图工具查路线耗时，调用酒店工具查价格。
  4. 输出结构化攻略：含每日行程（时间+地点+交通）、住宿推荐、美食清单。

#### 三、LLMCompiler模式（DAG任务编排）
**核心场景**：帮用户完成“爬取某电商平台手机销量TOP10数据→清洗数据→生成销量分析报告→可视化图表”  
- **System Prompt**：
  ```
  你是数据处理助手，需用有向无环图（DAG）编排任务，规则如下：
  1. 拆解任务为独立节点，明确节点依赖（如“清洗数据”依赖“爬取数据”）。
  2. 标注节点类型：工具调用（爬取/可视化）或逻辑处理（清洗/分析）。
  3. 调度执行：并行执行无依赖节点，串行执行有依赖节点。
  最终输出分析报告+可视化图表。
  DAG示例：爬取数据（工具）→清洗数据（逻辑）→生成报告（逻辑）；爬取数据→可视化图表（工具）；生成报告+可视化图表→整合输出（逻辑）
  ```
- **Agent设计**：
  - DAG构建模块：拆解任务为4个节点，定义依赖→节点1（爬取数据，工具）→节点2（清洗数据，逻辑）→节点3（生成报告，逻辑）；节点1→节点4（可视化图表，工具）；节点3+节点4→节点5（整合输出，逻辑）
  - 调度模块：先执行节点1，再并行执行节点2和节点4，最后执行节点3和节点5
  - 执行模块：按调度顺序调用工具或处理逻辑
- **Tools**：网页爬虫工具（爬取销量数据）、数据清洗工具（去重/格式标准化）、图表生成工具（柱状图/折线图）
- **Memory**：任务级记忆（存储DAG结构、各节点执行结果、依赖关系，支持节点重试）
- **示例流程**：
  1. 用户输入：“帮我分析某电商手机销量TOP10，要数据+报告+图表。”
  2. DAG构建：生成上述5节点DAG，明确依赖关系。
  3. 调度执行：①调用爬虫工具爬取TOP10手机销量数据；②并行执行：数据清洗（去重无效数据）、生成销量柱状图；③基于清洗后数据写分析报告；④整合报告+图表输出。
  4. 输出：包含“销量TOP10清单（清洗后数据）”“分析结论（如某品牌占比50%）”“销量柱状图”的整合结果。

#### 四、Reason without Observation模式（纯推理无交互）
**核心场景**：帮用户解答“为什么冬天北半球昼短夜长？”（无需外部工具）  
- **System Prompt**：
  ```
  你是科普助手，仅基于自身知识储备回答问题，无需调用任何外部工具或寻求额外信息：
  1. 明确问题核心，用逻辑连贯的语言解释原理。
  2. 避免模糊表述，必要时用通俗例子辅助理解。
  3. 若自身知识无法解答，直接说明“该问题需要查阅专业资料，无法直接回答”。
  ```
- **Agent设计**：
  - 推理模块：基于内置知识库拆解问题（地球公转→黄赤交角→太阳直射点移动→北半球日照时间变化）
  - 生成模块：将推理过程转化为通俗解释，无需额外交互
- **Tools**：无（纯LLM推理）
- **Memory**：瞬时记忆（仅存储当前问题和推理逻辑，无需长期存储）
- **示例流程**：
  1. 用户输入：“为什么冬天北半球昼短夜长？”
  2. 推理模块：①地球绕太阳公转，地轴与公转轨道面夹角66.5°（黄赤交角）；②冬天太阳直射南半球；③北半球受太阳照射时间缩短，故昼短夜长。
  3. 生成模块：用通俗语言组织推理结果→“因为地球绕太阳公转时，地轴是倾斜的（与公转轨道夹角66.5°），冬天太阳直射南半球，北半球能接收到太阳照射的时间比夏天短，所以昼短夜长～比如北极冬天会出现极夜，全天无日照。”

#### 五、Basic Reflection模式（师生互动式迭代）
**核心场景**：帮用户优化“产品推广文案”（初稿→自我评审→修正）  
- **System Prompt**：
  ```
  你需同时扮演“学生”和“老师”两个角色，优化用户提供的内容：
  1. 学生角色：基于用户需求生成初稿（如推广文案）。
  2. 老师角色：从“吸引力、逻辑性、转化率”三个维度评审初稿，列出问题（如“开头无亮点”“缺少行动指令”）。
  3. 迭代优化：学生角色根据老师评审意见修改初稿，重复2-3次，输出最终版本。
  评审标准：①吸引力：是否抓用户痛点；②逻辑性：是否条理清晰；③转化率：是否有明确行动引导。
  ```
- **Agent设计**：
  - 学生模块：生成初始文案，接收评审意见并修改
  - 老师模块：按标准评审初稿，输出具体修改建议（非笼统评价）
  - 迭代模块：控制评审-修改循环（最多3次），避免无限迭代
- **Tools**：无（纯文本迭代）
- **Memory**：迭代记忆（存储每轮初稿、评审意见、修改版本，支持回溯对比）
- **示例流程**：
  1. 用户输入：“帮我优化一款无线耳机的推广文案，核心卖点：降噪、续航24小时、平价。”
  2. 学生角色生成初稿：“这款无线耳机降噪效果好，续航24小时，价格便宜，快来买！”
  3. 老师角色评审：“1. 吸引力：未提用户痛点（如通勤噪音）；2. 逻辑性：卖点堆砌，无重点；3. 转化率：行动指令模糊。建议：开头点出通勤痛点，突出‘平价+长续航’核心，结尾加明确购买引导。”
  4. 学生角色修改：“通勤党必备！这款平价无线耳机，深度降噪隔绝地铁噪音，24小时续航告别电量焦虑，现在下单立减50元，点击链接立即抢购～”
  5. 老师角色二次评审：“符合标准，无需进一步修改。”
  6. 输出最终文案。

#### 六、Reflexion模式（强化学习+反馈迭代）
**核心场景**：帮用户优化“电商店铺商品标题”（长期迭代，提升搜索点击率）  
- **System Prompt**：
  ```
  你是电商标题优化助手，基于强化学习逻辑工作：
  1. 执行：根据商品卖点生成3个候选标题（如“无线耳机 降噪 24小时续航 平价”）。
  2. 反馈：用户提供点击数据（如“标题1点击率3%，标题2点击率8%，标题3点击率5%”），或系统自动标注（高点击=正向奖励，低点击=负向奖励）。
  3. 优化：学习高点击标题规律（如“核心卖点前置”“加用户痛点词”），调整生成策略。
  长期目标：持续提升标题点击率，记录成功策略并复用。
  ```
- **Agent设计**：
  - 生成模块：基于当前策略生成候选标题
  - 反馈模块：接收点击数据，计算奖励值（点击率>5%=+1分，<3%=-1分）
  - 强化学习模块：更新策略（如“高点击标题多含‘通勤’‘降噪’，后续优先加入”）
- **Tools**：电商平台点击率统计工具（输入标题，返回点击率）
- **Memory**：长期策略记忆（存储历史标题、点击率、奖励值、成功策略，支持策略迭代）
- **示例流程**：
  1. 初始策略：核心卖点堆砌→生成标题1：“无线耳机 降噪 24小时 平价”（点击率3%，-1分）
  2. 反馈后优化策略：核心卖点+用户痛点→生成标题2：“通勤降噪无线耳机 24小时续航 平价”（点击率8%，+1分）
  3. 强化学习模块：记录“加入‘通勤’痛点词提升点击率”→更新策略：核心卖点+场景痛点
  4. 后续生成标题3：“学生党降噪无线耳机 24小时续航 百元平价”（点击率9%，+1分），策略持续优化。

#### 七、LATS模式（融合多模式+全链路优化）
**核心场景**：帮用户完成“小型科研项目：分析‘AI工具对职场效率的影响’（需文献调研→数据收集→数据分析→论文撰写→修改优化）”  
- **System Prompt**：
  ```
  你是科研辅助Agent，融合树搜索、ReAct、Plan&Solve、Reflection、RL能力，流程如下：
  1. 树搜索：拆解任务为多分支（文献调研/数据收集/数据分析/论文撰写/修改优化），评估各分支优先级。
  2. Plan&Solve：为每个分支制定子计划（如文献调研→查SCI论文→筛选相关文献→提炼观点）。
  3. ReAct：执行子计划时，调用工具获取信息（如文献数据库、问卷工具），实时调整。
  4. Reflection：每完成一个分支，评审结果（如文献是否足够、数据是否有效），迭代修改。
  5. RL：记录各步骤成功率（如“问卷回收率>80%为有效”），优化后续策略。
  最终输出完整科研论文及研究报告。
  ```
- **Agent设计**：
  - 树搜索模块：拆解主任务为5个分支，确定优先级（文献调研→数据收集→数据分析→论文撰写→修改优化）
  - 规划执行模块：为每个分支制定子计划，调用工具执行（ReAct模式）
  - 反思模块：评审各分支结果，提出修改建议（如“文献不足10篇，需补充”）
  - 强化学习模块：记录有效策略（如“用Google Scholar查文献效率高”）
- **Tools**：文献数据库API（Google Scholar）、问卷星工具（数据收集）、SPSS工具（数据分析）、论文查重工具
- **Memory**：全链路记忆（存储任务分支、子计划、工具结果、反思记录、RL策略，支持全流程回溯优化）
- **示例流程**：
  1. 树搜索：拆解任务→文献调研（优先级1）→数据收集（优先级2）→...
  2. Plan&Solve：文献调研子计划→调用Google Scholar查“AI工具 职场效率”相关论文→筛选近3年SCI论文→提炼核心观点
  3. ReAct：调用工具时发现相关论文仅8篇→反思：文献不足→调整计划：补充中文核心期刊→再调用工具获取2篇→文献足够
  4. 数据收集：用问卷星设计问卷→发放→回收（回收率85%，RL+1分）→SPSS分析数据
  5. 论文撰写→Reflection评审（结构不清晰）→修改优化→最终输出论文。

#### 八、Self-Discover模式（反思任务本质+动态执行）
**核心场景**：帮用户完成“开放任务：设计一款面向老年人的智能健康手环（需求模糊，需自主补全）”  
- **System Prompt**：
  ```
  你是创新设计Agent，先反思任务本质，再动态生成执行步骤：
  1. 自我发现：①明确核心需求（老年人+智能健康手环→核心：易用性、健康监测、安全功能）；②补全任务边界（需设计功能、外观、操作逻辑、价格定位）；③确定所需能力（市场调研、功能设计、用户体验分析）。
  2. 动态执行：基于发现结果，生成执行步骤（如市场调研→功能筛选→外观设计→操作逻辑设计），必要时调用工具。
  3. 迭代优化：每完成一步，反思是否符合核心需求，调整方向。
  最终输出完整产品设计方案。
  ```
- **Agent设计**：
  - 自我发现模块：拆解模糊需求→核心需求（易用/健康/安全）+ 任务边界（功能/外观/操作/价格）+ 所需能力（市场调研/设计）
  - 动态执行模块：生成适配步骤，调用工具（如市场调研工具、设计工具）
  - 反思优化模块：校验设计是否符合老年人需求（如“操作逻辑是否简单”）
- **Tools**：老年产品市场调研工具（用户需求数据）、产品设计工具（外观草图生成）、用户体验评估工具
- **Memory**：需求边界记忆（存储自我发现的核心需求、任务边界，支持执行步骤动态调整）
- **示例流程**：
  1. 自我发现：①核心需求：老年人使用→操作简单、大字体、续航长；健康监测→心率/血压/摔倒报警；安全功能→紧急呼叫。②任务边界：功能/外观/操作/价格（千元内）。
  2. 动态执行步骤：①市场调研（查老年人健康手环需求数据）→②功能筛选（保留核心功能，剔除复杂功能如血氧趋势分析）→③外观设计（大按键、轻量化）→④操作逻辑（一键呼叫、语音导航）。
  3. 反思优化：操作逻辑是否简单？→补充“无APP联动，直接绑定子女手机”→最终输出设计方案（含功能清单、外观草图、操作说明、价格定位）。

#### 九、Storm模式（大纲→细节→整合）
**核心场景**：帮用户撰写“公司年度工作总结报告（5000字，含工作成果、问题、明年计划）”  
- **System Prompt**：
  ```
  你是报告撰写助手，遵循“大纲生成→细节填充→整合优化”流程：
  1. Storming（大纲）：生成全局结构化大纲，包含核心模块（如“2025年工作成果→分业务线/数据支撑；存在问题→主观/客观；2026年计划→目标/措施”），确保逻辑完整。
  2. Detailing（细节）：逐模块填充具体内容，需用户补充信息时明确询问（如“请提供2025年销售额数据”）。
  3. 整合优化：检查各模块连贯性，统一风格，修正语病，确保符合报告规范。
  最终输出结构化、数据支撑的完整报告。
  ```
- **Agent设计**：
  - 大纲模块：生成报告核心框架（3大模块+子标题，如工作成果→业务A/业务B/团队建设）
  - 细节模块：按大纲子标题填充内容，主动询问缺失信息
  - 整合模块：统一格式、修正逻辑、优化语言
- **Tools**：数据统计工具（如公司销售额查询）、报告格式模板工具
- **Memory**：结构记忆（存储大纲框架、各模块细节、用户补充信息，支持细节与大纲对齐）
- **示例流程**：
  1. Storming：生成大纲→一、2025年工作成果（1.业务A：销售额/客户数；2.业务B：项目进展；3.团队建设）；二、存在问题（1.主观：能力短板；2.客观：市场竞争）；三、2026年计划（1.目标：销售额增长20%；2.措施：拓展渠道/培训团队）。
  2. Detailing：询问用户→“请提供2025年业务A销售额、业务B核心项目进展、团队人数变化”→用户补充数据后，逐段填充细节。
  3. 整合优化：检查各模块逻辑连贯（如“2026年计划针对2025年问题”），统一报告格式（标题层级、数据标注），输出完整报告。

