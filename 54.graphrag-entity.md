GraphRAG 的核心在于利用 LLM（如 GPT-4）的推理能力，将原本琐碎的非结构化文本，通过一套严密的“抽取-精炼-对齐”工作流，转化为结构化的知识图谱。

为了讲清楚这个过程，我们假设有一段原始文本：
> “伊隆·马斯克（Elon Musk）是特斯拉的 CEO。这位出生于南非的企业家最近访问了中国。与此同时，库克（Tim Cook）在加州总部讨论了苹果公司的未来，苹果这家科技巨头正准备推出新的 AI 功能。”

---

### 第一步：多轮抽取（Multi-round Extraction / Gleaning）

GraphRAG 不会只让 LLM 读一遍就结束，因为 LLM 在处理长文本时容易产生“注意力缺失”。

1.  **首轮抽取（Initial Extraction）：**
    使用精心设计的 Prompt，要求 LLM 提取所有实体（Entity）和关系（Relationship）。
    *   **Prompt 逻辑：** “请从文本中提取所有人物、组织、地点和概念。对于每个关系，识别主体、谓语和宾语。”
    *   **初次结果：** 提取出“马斯克”、“特斯拉”、“库克”、“苹果公司”。

2.  **增量抽取/拾遗（Gleaning）：**
    GraphRAG 会问 LLM 第二遍：“在这个文本块中，是否还有遗漏的、更隐蔽的实体或关系没有被提取？请继续列出。”
    *   **拾遗结果：** LLM 可能会发现“南非”（地点）、“AI 功能”（技术概念）、“加州总部”（地点）。
    *   **停止条件：** 当 LLM 返回“没有更多实体”或者达到预设的轮次数（通常2轮）时停止。这样可以最大化召回率。

---

### 第二步：实体的 Label 与 Attribute 是怎么抽取的？

在抽取的过程中，LLM 会按照 Schema（模式）要求生成结构化数据：

*   **实体 Label（类型）：** LLM 根据上下文自动分类。例如，“特斯拉”的 Label 是 `ORGANIZATION`，“马斯克”是 `PERSON`。
*   **实体 Attributes（属性）：** GraphRAG 并不像传统数据库那样通过固定字段（如 age, gender）来抽，而是将**所有关键描述（Description）**作为最重要的属性。
    *   *例子：* 对于“特斯拉”，属性描述为“一家由马斯克领导的、最近在关注中国市场的电动汽车公司”。
*   **关系抽取（Source & Target）：**
    LLM 识别动作的发出者和承受者。
    *   *例子：* “马斯克访问了中国”。
    *   *结果：* `Source: 马斯克 | Target: 中国 | Relationship: 访问 | Weight: 1（强度）`

---

### 第三步：实体对齐与消歧（Resolution & Disambiguation）

这是最难的一步。文本中会出现“马斯克”、“伊隆·马斯克”、“这位企业家”、“Musk”，它们其实是同一个实体。

1.  **指代消解（Coreference Resolution）：**
    LLM 在处理同一个文本块时，能够理解“这位企业家”指的就是前文的“马斯克”。在抽取阶段，LLM 会自动将它们统一归位到“马斯克”这个 ID 下。

2.  **名称归一化（Naming Normalization）：**
    如果不同的文本块分别出现了 "Elon Musk" 和 "马斯克"，GraphRAG 会通过**实体描述（Description）**来对齐。
    *   系统会对比两个实体的描述。如果两个实体的描述极其相似（例如都关联了特斯拉、CEO 等关键词），LLM 会执行一个 **"Merge"** 操作，将两个节点合并。

3.  **处理二义性（Ambiguity）：**
    如果文本中出现了两个“苹果”：
    *   语境 A：“苹果发布了 iPhone。” -> 描述：科技公司。
    *   语境 B：“由于干旱，苹果减产了。” -> 描述：农产品水果。
    *   **解决：** 因为它们的描述（Description）和所在社区（邻居节点）完全不同，GraphRAG 会根据 **语义向量（Embedding）** 和 **上下文描述** 将其判定为两个独立的实体，赋予不同的 Unique ID。

---

### 第四步：具体例子流程演示

**输入文本：** “马斯克领导着特斯拉。Elon 昨天去了北京。与此同时，苹果公司在库克的带领下研发 AI。”

| 步骤 | 操作内容 | 产出结果 |
| :--- | :--- | :--- |
| **抽取 R1** | 识别明显实体 | 实体：马斯克、特斯拉、Elon、北京、苹果公司、库克、AI |
| **抽取 R2 (Gleaning)** | 挖掘深层关系 | 关系：(马斯克, 领导, 特斯拉), (库克, 领导, 苹果公司) |
| **Label 标注** | 分类 | 马斯克: PERSON, 特斯拉: ORGANIZATION, AI: CONCEPT |
| **消歧/对齐** | 判断 马斯克 == Elon | **合并实体**。将 "Elon" 的描述合并到 "马斯克" 节点下。 |
| **属性填充** | 聚合描述 | 马斯克属性：领导特斯拉、去了北京的人物。 |
| **源/目确定** | 确定边方向 | 源: 马斯克 -> 关系: 访问 -> 目的: 北京 |

---

### 总结：GraphRAG 靠什么做到的？

1.  **靠 Prompt Engineering（提示词工程）：** 强制 LLM 以 `Entity <|> Relationship` 的格式输出，并要求其在每一行中包含详细的描述。
2.  **靠 Description（描述）驱动：** 传统的图谱靠 Key-Value，GraphRAG 靠 LLM 生成的长文本描述。**描述是消歧和对齐的“指纹”**。
3.  **靠多轮迭代：** 解决了单次抽取由于 Token 限制导致的漏抽问题。
4.  **靠图算法（Leiden）：** 在抽取完成后，通过 Leiden 算法聚类。如果两个实体原本应该对齐但没对齐，在聚类时它们往往会被分到同一个“社区”，在后续的“社区总结（Community Summary）”阶段，LLM 会再次发现并将其逻辑统一。