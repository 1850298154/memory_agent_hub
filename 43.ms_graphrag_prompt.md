# 微软的Graphrag中这些prompt使用的顺序，以及作用、完成的功能，如果不使用会产生什么问题？使用之后解决什么问题？给我输出一个大表格：
basic_search_system_prompt.txt             
global_search_map_system_prompt.txt
claim_extraction.txt                       
global_search_reduce_system_prompt.txt
community_report.txt                       
local_search_system_prompt.txt
drift_search_system_prompt.txt             
question_gen_system_prompt.txt
entity_extraction.txt                      
summarize_descriptions.txt
global_search_knowledge_system_prompt.txt

以下是微软GraphRAG中各prompt的使用顺序、作用、功能、不使用的问题及使用后的解决效果整理表格：

| **Prompt文件名**                              | **使用顺序** | **作用**                                                                 | **完成的功能**                                                                 | **不使用会产生什么问题**                                                                 | **使用之后解决什么问题**                                                                 |
|---------------------------------------------|------------|----------------------------------------------------------------------|-----------------------------------------------------------------------------|-------------------------------------------------------------------------------------|-------------------------------------------------------------------------------------|
| entity_extraction.txt                       | 1          | 从文本中提取实体（如人物、组织、地点等）                                  | 识别文本中的核心实体，建立实体基础库，为后续知识关联提供基础                         | 实体识别缺失，后续知识组织混乱，无法建立实体间关联，导致知识碎片化                         | 精准提取实体，为知识图谱构建提供核心节点，确保知识结构化基础                               |
| claim_extraction.txt                        | 2          | 从文本中提取主张（事实性陈述、观点等）                                    | 提取实体相关的事实或观点，作为实体间关系的基础信息                                 | 无法捕捉实体间的关键信息，知识图谱缺乏核心内容，回答问题时缺乏事实依据                     | 明确实体相关的事实主张，为关系构建和推理提供内容支撑                                     |
| summarize_descriptions.txt                  | 3          | 对实体或主张的描述进行汇总和精简                                          | 压缩冗余信息，生成简洁的实体/主张摘要，减少信息过载                               | 原始信息冗长杂乱，后续处理效率低，用户难以快速获取核心内容                                 | 精简信息，提高知识处理效率，便于后续检索和展示                                           |
| question_gen_system_prompt.txt              | 4          | 基于提取的实体和主张生成相关问题                                          | 生成用于验证或扩展知识的问题，驱动进一步信息检索或推理                             | 知识覆盖不全面，缺乏主动探索机制，可能遗漏关键关联信息                                   | 通过问题引导知识扩展，补充潜在缺失信息，增强知识完整性                                     |
| local_search_system_prompt.txt              | 5          | 在局部知识社区（实体集群）内进行检索                                      | 针对特定实体集群内的信息进行精准查找，聚焦局部关联知识                             | 局部知识关联被忽略，检索范围过大导致噪音增多，难以找到针对性信息                           | 提高局部知识检索精度，强化实体集群内的关联挖掘                                           |
| global_search_knowledge_system_prompt.txt   | 6          | 构建全局知识检索的基础框架（如知识地图）                                  | 定义全局知识的组织结构和检索规则，为跨社区检索提供基础                             | 全局知识结构混乱，跨社区检索无规则可依，无法实现知识的整体关联                             | 建立全局知识检索框架，支撑跨社区的知识关联和整合                                           |
| global_search_map_system_prompt.txt         | 7          | 基于全局知识框架生成知识地图                                              | 可视化全局知识分布和关联，展示实体/主张的全局位置和关系                           | 全局知识关联不直观，难以把握知识整体结构，跨领域检索效率低                                 | 通过知识地图直观呈现全局关联，提升跨领域知识检索的效率和可理解性                           |
| global_search_reduce_system_prompt.txt      | 8          | 对全局检索结果进行聚合和精简                                              | 合并重复或冗余的全局检索结果，提炼核心信息                                       | 全局检索结果杂乱重复，信息过载，用户难以快速获取有效内容                                 | 精简全局检索结果，突出核心信息，提升信息利用效率                                           |
| drift_search_system_prompt.txt              | 9          | 检测知识随时间或语境的漂移（如实体含义变化、主张更新）                      | 识别知识的动态变化，确保知识时效性和准确性                                       | 知识陈旧或过时，无法反映最新状态，可能导致错误推理或回答                                   | 及时捕捉知识漂移，更新知识库，保证知识的时效性和可靠性                                     |
| basic_search_system_prompt.txt              | 10         | 提供基础检索能力，作为其他检索的补充或 fallback                            | 处理简单检索需求，在复杂检索失效时提供基础结果                                   | 复杂检索失败时无备选方案，导致用户无法获取任何信息                                         | 保障基础检索需求，提高系统鲁棒性，避免检索完全失效                                         |
| community_report.txt                        | 11         | 生成知识社区（实体集群）的汇总报告                                        | 整合社区内的实体、主张及关联，输出结构化报告，便于用户理解社区知识概况             | 知识社区信息分散，用户难以快速掌握社区核心内容，知识利用门槛高                             | 提供社区知识全景报告，降低知识理解门槛，提升知识应用效率                                   |

### 说明：
1. 顺序基于GraphRAG的知识处理逻辑（从实体/主张提取→局部处理→全局整合→动态优化→应用输出）推导，实际可能根据场景微调。
2. 各prompt通过分工协作实现“从原始文本到结构化知识→检索→应用”的全流程，缺一环节可能导致知识处理链条断裂或效果下降。


# 在 graph rag 中会对哪些内容做 embedding？会充值到哪些文件中？

在 Graph RAG（图增强检索增强生成）中，**Embedding 的核心目标是将“图中的结构化/非结构化信息”转化为可计算的向量**，以便后续高效检索（如相似性匹配、图邻居召回融合）；而“存储”则分为向量存储（用于快速检索）和图存储（用于保存结构化关系）两类，不存在“充值到文件”的说法，更多是持久化到数据库/存储系统中。以下是具体拆解：


### 一、Graph RAG 中需要做 Embedding 的核心内容
Graph RAG 的本质是“图结构 + 非结构化文本”的融合，因此 Embedding 会覆盖**图的节点、边、关联的非结构化文本**三类核心对象，目的是让“结构化关系”和“文本语义”都能通过向量参与检索。

#### 1. 节点（Node）相关 Embedding
节点是 Graph RAG 的核心载体，几乎所有节点都需要嵌入，具体分为：
- **实体节点（Entity Node）**：
  - 内容：现实世界中的具体对象（如人名、公司名、产品名、概念名，例：“张三”“字节跳动”“大模型”）。
  - 嵌入方式：
    - 基础嵌入：直接对实体名称做 Embedding（如“字节跳动”→ 向量）；
    - 增强嵌入：结合实体的“属性信息”（如“字节跳动，成立于2012年，总部北京”）拼接后做 Embedding，让向量更具语义区分度。
  - 目的：快速召回与查询相关的核心实体（如查询“字节跳动的产品”，先通过向量匹配找到“字节跳动”节点）。

- **文档/段落节点（Document/Chunk Node）**：
  - 内容：原始非结构化文本的拆分单元（如一篇文章拆成的段落、句子，例：“大模型是人工智能的重要方向，具有强语义理解能力”）。
  - 嵌入方式：直接对段落/句子文本做 Embedding（这是传统 RAG 的核心嵌入逻辑，Graph RAG 会复用并关联到图中）。
  - 目的：保留传统 RAG 的文本语义检索能力，同时通过图关联实体，提升召回的相关性。

- **概念/类别节点（Concept/Category Node）**：
  - 内容：抽象的分类或概念（如“人工智能”“互联网公司”“技术方向”）。
  - 嵌入方式：对概念名称+描述（如“人工智能：研究让机器模拟人类智能的技术”）做 Embedding。
  - 目的：支持“概念级检索”（如查询“人工智能相关的公司”，先匹配“人工智能”概念节点，再通过图关系找到关联的公司实体）。

#### 2. 边（Edge）相关 Embedding（可选，按需使用）
边代表节点间的关系（如“张三-任职于-字节跳动”“大模型-属于-人工智能”），是否做 Embedding 取决于场景：
- 内容：关系类型名称（如“任职于”“属于”“合作”）+ 关系描述（可选，如“张三于2020年任职于字节跳动，担任算法工程师”）。
- 嵌入方式：对“关系名+描述”拼接文本做 Embedding。
- 目的：
  - 区分同类型但语义不同的关系（如“合作”可能是“技术合作”或“商业合作”，通过嵌入区分）；
  - 支持“关系级检索”（如查询“字节跳动与哪些公司有技术合作”，通过向量匹配“技术合作”边，筛选关联节点）。
- 注意：简单场景下（如关系类型少、无复杂描述），边可仅用标签（如“任职于”）标识，无需做 Embedding。

#### 3. 辅助性 Embedding（可选）
- **图子结构嵌入（Subgraph Embedding）**：对某个局部子图（如“张三-任职于-字节跳动-开发-抖音”）的整体语义做 Embedding，用于检索“结构化+语义”结合的子图（如查询“谁在字节跳动参与短视频产品开发”）。
- **查询（Query）Embedding**：用户查询本身也需要做 Embedding，用于与上述节点/边/子图的向量做相似性匹配，实现检索召回。


### 二、Embedding 及相关数据的存储目标（不是“充值到文件”，是持久化到系统）
Graph RAG 的存储是“**向量存储 + 图存储**”的双存储架构，Embedding 主要存于向量存储，图结构（节点/边的元数据）存于图存储，二者通过唯一标识（如节点 ID）关联。

#### 1. 向量存储（核心存 Embedding 向量）
- 存储内容：所有做过 Embedding 的对象的向量（节点向量、边向量、子图向量），以及对应的“元数据”（如向量所属节点 ID、原始文本、属性信息）。
- 常用存储系统：
  - 专用向量数据库：Milvus、Zilliz Cloud、Pinecone、Weaviate（支持高维向量的快速相似性检索，如 FAISS 算法优化）；
  - 轻量方案：FAISS（本地向量索引文件）、Chroma（嵌入式向量库，适合小规模场景）。
- 核心作用：接收用户查询的 Embedding 向量，快速召回Top-K 相似的节点/边向量，为后续图遍历、答案生成提供候选集。

#### 2. 图存储（核心存结构化关系）
- 存储内容：Graph RAG 的“图结构元数据”，包括：
  - 节点：节点 ID、实体/文档/概念名称、属性（如“字节跳动”的成立时间、总部）、关联的文档片段 ID；
  - 边：边 ID、起点/终点节点 ID、关系类型（如“任职于”）、关系描述（可选）。
- 常用存储系统：
  - 图数据库：Neo4j（原生图数据库，支持复杂图遍历）、NebulaGraph、ArangoDB（多模型数据库，支持图+文档）；
  - 轻量方案：NetworkX（Python 图计算库，适合内存级小规模图）。
- 核心作用：保存节点间的结构化关系，支持“邻居召回”（如找到“字节跳动”的所有关联产品节点）、“路径查询”（如“张三→任职于→字节跳动→开发→抖音”），与向量存储的召回结果融合，提升检索的完整性。

#### 3. 辅助存储（可选，存原始文本）
- 存储内容：原始非结构化文本（如完整文档、未拆分的段落），避免重复存储在向量/图存储中。
- 常用存储系统：文件系统（本地文件夹、S3）、文档数据库（MongoDB）。
- 核心作用：当向量存储召回节点后，通过节点 ID 关联到原始文本，用于后续的上下文拼接和 LLM 生成答案。


### 总结
1. **Embedding 对象**：核心是节点（实体、文档、概念），可选边、子图、查询，本质是将“结构化关系”和“非结构化文本”转化为向量；
2. **存储逻辑**：Embedding 向量存于向量存储（用于快速相似性检索），图结构（节点/边元数据）存于图存储（用于结构化关系遍历），二者联动实现 Graph RAG 的“语义检索+关系推理”能力，无“充值到文件”的说法，而是持久化到专业数据库/索引系统中。
