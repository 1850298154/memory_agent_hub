[从 RAG 到 Context：2025 年 RAG 技术年终总结](https://mp.weixin.qq.com/s/QzXcdYnV68pZF5yROlipPA?from=groupmessage&scene=1&subscene=10000&clicktime=1766675856&enterid=1766675856&sessionid=0&ascene=1&fasttmpl_type=0&fasttmpl_fullversion=8055889-en_US-zip&fasttmpl_flag=0&realreporttime=1766675856592)
# 2025年RAG技术年终总结：从检索工具到上下文平台的演进
## 一、2025年RAG技术整体态势
2025年RAG技术虽受“临时性”“被替代性”争议，且舆论关注度被AI Agents分流，但在企业级AI落地中愈发关键，中大型企业对其投入更深入系统化，仍是企业AI架构的核心数据基础设施。

## 二、核心争议：长上下文与RAG的关系
### （一）长上下文的局限性
- **效果问题**：在LLM上下文窗口机械填充过长文本，会导致模型注意力分散，出现“中间迷失”或“信息淹没”效应，回答质量下降。
- **成本问题**：处理长上下文的计算开销呈非线性增长，成本高昂。

### （二）二者协同模式
并非“取代”而是“协同”，形成“检索前置，长上下文容纳”模式。如RAG系统用长上下文窗口容纳完整检索结果块，或汇总多步检索与反思中间结果，催生“上下文工程”领域，工作重心转向“检索-上下文组装-模型推理”端到端链路设计。

### （三）四种外部知识输入范式成本对比
|输入范式|成本情况|关键限制|
| ---- | ---- | ---- |
|单纯依赖LLM长上下文能力|与完整RAG架构相差2个数量级|效果差、成本高|
|借助KV Cache|成本比RAG高至少1个数量级|大数据量与检索性能矛盾、场景局限|
|利用Grep等简易搜索手段|成本低|仅适用于结构化数据，无法处理多模态、非结构化数据|
|采用完整RAG架构|成本低|无显著关键限制，是复杂企业级需求的综合解决方案|

## 三、RAG对话效果优化路径
### （一）传统RAG的结构性矛盾
传统“分块-嵌入-检索”流水线中，单一粒度文本块需同时满足“语义匹配（小文本块，100-256Token，精准）”与“上下文理解（大文本块，1024Token以上，完整）”，导致顾此失彼。

### （二）优化方案：“搜索-检索”两阶段解耦
1. **搜索阶段**：类比“扫描定位”，用小粒度、语义纯净文本单元，实现高召回率与高精度。
2. **检索阶段**：类比“阅读理解”，基于搜索线索，动态聚合、拼接出大而完整的上下文片段。

### （三）典型技术实践
- **TreeRAG**：离线用LLM将文档生成多级树状目录摘要，在线先以细粒度片段搜索，再依树状目录组合语义相关片段，解决上下文碎片化问题。
- **PageIndex**：依赖文档固有目录结构，文档结构清晰时高效，反之则定位能力弱。
- **GraphRAG**：抽取实体及关系构建知识图谱，辅助发现跨文档关联信息，但存在Token消耗大、实体抽取质量低、知识碎片化问题。
- **混合架构**：结合TreeRAG（解决局部语义断裂）与GraphRAG（发现跨文档关联）优势，引入语义增强与结构构建层，提升复杂问答能力。

## 四、RAG的定位演进：从知识库到数据底座
### （一）核心转变：成为Agent数据基座
随AI Agent发展，RAG从“问答知识库”转向企业级非结构化数据统一处理与访问平台，为Agent提供统一、高效、安全的非结构化数据访问服务。

### （二）关键组件：数据注入管道（Ingestion pipeline/PTI）
类比结构化数据的ETL/ELT，处理非结构化数据，核心环节与ETL对比：
|环节|ETL（结构化数据）|PTI（非结构化数据）|
| ---- | ---- | ---- |
|数据提取|从关系型数据库、API等抽取|强化解析（Parsing），用多种解析器将多模态文档转为结构化文本并保留逻辑与元信息|
|数据转换|基于SQL/代码清洗、标准化、计算|以LLM为核心，进行分块、树状结构生成、知识图谱抽取等语义增强处理|
|数据加载|加载到数据仓库/数据湖|通过Indexing构建支持混合检索的高效索引|

### （三）核心设计原则
优秀RAG系统需协同设计“数据注入-语义增强-索引构建-检索服务”全链路，而非仅依赖单一组件。

## 五、RAG与Context Engineering：Agent时代的新角色
### （一）Context Engineering的核心
解决“智能筛选、排序、拼接上下文”问题，避免信息过载损害LLM能力，Agent上下文需组装三类数据，均依赖检索技术：
- **领域知识数据**：RAG是领域知识上下文工程1.0，检索质量决定答案质量。
- **工具数据**：需检索工具描述及使用指南，动态筛选相关工具放入上下文，解决“工具选择”难题，MCP协议仅解决“如何调用”，检索解决“调用哪个”。
- **会话和状态数据**：即“记忆（Memory）”，核心是对历史交互信息的存储、索引与检索，与RAG技术同源，处理动态交互数据。

### （二）RAG的升级：上下文引擎（Context Engine）
RAG扩展数据范畴，涵盖领域文档检索（RAG）、会话记忆检索（Memory）、工具检索（Tool Retrieval），成为支撑Agent上下文组装的统一上下文层与数据底座。

### （三）Agent时代检索的新要求
与传统搜索相比，Agent检索发起者和初级消费者是Agent，需满足高请求频率（超传统1-2个数量级）、多样化查询类型、苛刻延迟、与推理流程紧密耦合的要求，需构建智能中间服务层协调检索与结果处理。

## 六、多模态RAG进展
### （一）技术价值
- **优势场景**：图文结合任务（视觉上下文检索、图像问答），表现优于纯文本方案。
- **局限场景**：纯文本任务（摘要生成、纯文本检索），不及成熟单模态RAG。

### （二）两大技术路径
|路径|实现方式|优缺点|
| ---- | ---- | ---- |
|模态转换路径|用OCR/VLM将图像、表格转为文本|兼容现有文本RAG，但丢失视觉关键信息|
|原生多模态路径|图像与文本一同输入多模态编码器生成多向量，用延迟交互模型计算相似度|保留细粒度语义信息，但工程化难度大|

### （三）工程化瓶颈与突破方向
- **瓶颈**：Token数量爆炸导致张量存储与计算开销激增，百万页文档库索引达TB级。
- **突破方向**：张量量化压缩（降低存储，需鲁棒嵌入模型）、Token剪枝（减少Token数量，如随机投影、聚类、模型端剪枝）。

## 七、2026年展望
- **技术方向**：RAG将彻底升级为“上下文引擎”，成为企业AI基础设施核心，多模态RAG随基础设施完善与模型优化有望普及，多模态记忆系统进入原型研发。
- **企业落地**：企业AI关注点转向规模化落地与投资回报，以RAG为核心的“AI中台”“智能数据底座”建设加速，上层Agent部署更灵活。

